# Sellin TN Environment Configuration

# Application Settings
NODE_ENV=production
PORT=3000

# Domain Configuration
DOMAIN=sellin.tn
PROTOCOL=https

# File Storage Configuration
STORES_DIR=./stores
STORES_DATA_FILE=./stores-data.json

# Security Settings
API_RATE_LIMIT=10
GENERAL_RATE_LIMIT=30

# Optional: Database Configuration (for future enhancements)
# DATABASE_URL=postgresql://user:password@localhost:5432/sellintn

# Optional: Email Configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: SSL Configuration
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key

# Optional: Analytics and Monitoring
# ANALYTICS_ID=your-analytics-id
# SENTRY_DSN=your-sentry-dsn 